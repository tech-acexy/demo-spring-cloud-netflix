server:
  port: 8080

spring:
  application:
    name: ribbon-client

eureka:
  client:
    service-url:
      defaultZone: http://acexy-tech.eureka:7000/eureka
  instance:
    instance-id: ${spring.application.name}-${random.int[1000,9999]}
    prefer-ip-address: true
